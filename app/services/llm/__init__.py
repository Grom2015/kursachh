"""LLM integration layer.

This package currently exposes:
- safe JSON payload builders used by the existing pipeline;
- dormant provider/client/prompt modules merged for future LLM reporting work.

The active product flow still does not invoke external LLM providers by default.
"""
