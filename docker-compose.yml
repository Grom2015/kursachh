services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: moex_research
      POSTGRES_USER: moex
      POSTGRES_PASSWORD: moex
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

